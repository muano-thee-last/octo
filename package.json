{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "passport": "^0.7.0",
    "passport-google-oauth2": "^0.2.0"
  }
}
